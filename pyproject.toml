[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sb_utils"
version = "0.1.0"
description = "A collection of utilities for modding Subway Builder."
dependencies = [
    "requests>=2.25.1",
]

[tool.setuptools.packages.find]
where = ["src"]
